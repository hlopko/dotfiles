[general]
pythonfile = ~/.offlineimap.py
accounts = fastmail
socktimeout = 60
keepalive = 60
maxsyncaccounts = 3
#ui = quiet
fsync = False

[Account fastmail]
localrepository  = fastmailLocal
remoterepository = fastmailRemote
status_backend   = sqlite
#autorefresh      = 5
#quick            = 10

[Repository fastmailLocal]
localfolders = ~/.mail/fastmail
type         = Maildir
#readonly     = True

[Repository fastmailRemote]
type               = IMAP
remotehost         = mail.messagingengine.com
remoteport         = 993
ssl                = yes
sslcacertfile      = /etc/ssl/certs/ca-certificates.crt
holdconnectionopen = yes
maxconnections     = 3
#readonly           = True
realdelete         = no
remoteusereval     = get_username("fastmail") + "@fastmail.fm"
remotepasseval     = get_password("fastmail")
folderfilter       = lambda folder: not re.search('(^RESTORED)', folder)
