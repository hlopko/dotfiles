#!/usr/bin/env ruby

if ARGV.size == 0
  puts "Usage"
	puts "rewrap_mts_to_mp4 <infile> <outfile>"
else
  puts "INFILE: #{ARGV[0]}"
  puts "OUTFILE: #{ARGV[1]}"
	`avconv -i #{ARGV[0]} -vcodec copy -acodec copy #{ARGV[1]}`
end
